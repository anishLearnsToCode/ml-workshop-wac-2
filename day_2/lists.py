"""
lists / array

- mutable (we can modify it)
- iterable
[]
"""

numbers = [2, 3, 5, 7, 11, 13]
print(numbers)
print(type(numbers))

# add elements
# append(value)
# insert(position, value)

# Remove elements
# pop(index=-1)
# remove(value)
# del keyword
